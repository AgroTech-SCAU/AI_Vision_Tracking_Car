# v0.1 视觉跟随项目：现有性能测试汇总

整理日期：2026-09-23。本文汇总项目中已有的测试报告，供三模型同时运行的主板与相机选型使用。所有结论只对应报告中的软件、电脑和输入条件；本次整理没有重新运行模型。

## 1. 先看结论

- **三模型合跑、模型目标 15 FPS：**模拟 RGB 60 FPS、深度 30 FPS，持续 300 秒；4,500 帧进入模型且全部处理，回放队列覆盖 **0 帧**，实际处理 **15.00 FPS**。送帧到结果的 P95 为 **49.44 ms**。仍有 **4 帧**的结果超过下一个 15 FPS 处理周期（66.67 ms），因此不能称为每帧都有严格的截止时间保证。[原始报告](realtime_replay_results/all_tennis_1280x800_30fps_20260923_225114_891918/report.json)
- **三模型合跑、模型目标 30 FPS：**9,000 帧进入模型，处理 8,094 帧，队列覆盖 **906 帧（10.07%）**，实际处理 **26.98 FPS**。三模型计算 P95 为 **51.22 ms**，送帧到结果 P95 为 **72.28 ms**，均无法满足稳定的 33.33 ms 处理周期。[原始报告](realtime_replay_results/all_tennis_1280x800_30fps_20260923_224339_907032/report.json)
- **三模型合跑、每个 RGB 帧都尝试处理（60 FPS）：**18,000 帧进入模型，处理 8,834 帧，队列覆盖 **9,166 帧（50.92%）**，实际处理 **29.44 FPS**。[原始报告](realtime_replay_results/all_tennis_1280x800_30fps_20260923_222949_456338/report.json)
- 上述“零覆盖/10% 覆盖/51% 覆盖”描述的是**电脑上的视频回放队列**，不是相机或 USB 丢帧。测试电脑采用 **RTX 5060 Laptop GPU + PyTorch**；不能直接换算为 Jetson FPS、TOPS、功耗或内存需求。
- **宠物动作模型训练使用 `imgsz=960`，这里的统一性能测试全部使用 `imgsz=640`。**如果最终需要 960 才能保持识别效果，当前耗时不能作为最终负载上限。[训练参数](pet_action_v0.1/runs/train-4/args.yaml)

## 2. 测试范围与环境

| 模块 | 模型与训练尺寸 | 本次性能测试尺寸 | 权重文件大小 | 功能 |
| --- | --- | ---: | ---: | --- |
| 猫狗检测 | YOLO11n，640 | 640 | 5.21 MB | 猫狗目标检测 |
| 宠物动作 | YOLO26n，960 | 640 | 5.23 MB | 宠物动作检测 |
| 运动员检测与跟踪 | YOLO11n，640 | 640 | 5.20 MB | 人物检测 + ByteTrack 分配 ID |

模型类型和训练尺寸来自各自的[猫狗训练参数](cat_dog_v0.1/runs/args.yaml)、[动作训练参数](pet_action_v0.1/runs/train-4/args.yaml)和[运动员训练参数](yolo_bytetrack_v0.1/runs/person_detection/0919_0014/args.yaml)。三个权重文件合计约 **15.64 MB**；文件大小不能代表运行时内存或每帧计算量。

所有下列标准性能报告均记录 Windows 10、Python 3.10.20、Ultralytics 8.4.93、PyTorch 2.11.0+cu128、NVIDIA GeForce RTX 5060 Laptop GPU、约 16 GB 主机 RAM。模型调用前后同步 CUDA；计时包含 YOLO 预处理、推理和后处理，运动员模式还包含 ByteTrack。各测试没有绘制、显示或保存输出视频。CPU 频率是采样值，不能单凭该数字推断 CPU 瓶颈。

使用了两类输入：

1. **离线文件测试：**宠物视频 `540×960 / 30 FPS / 778 帧`；运动员另用网球视频 `1280×592 / 约 29.86 FPS / 1492 帧`。离线吞吐量包含文件读取与解码，不等于实时相机延迟。
2. **按时钟回放测试：**[1280×800 网球片段](test_video/README.md)由 `812×720、约 10.44 秒`的示例视频缩放补边得到，标称 30 FPS、313 帧。60 FPS 回放时会重复原画面；300 秒测试约循环该片段 28 次。深度是复制的 `1280×800`、16 位固定数组，不是真实深度图。

## 3. 离线单模型与宠物双模型

下表中的“模型调用”包含预处理、推理、后处理；网球模型还包含 ByteTrack。P95 表示 95% 的被测帧不超过该耗时。不同视频的结果不能用于严格比较识别准确率或场景复杂度。

| 测试 | 视频 / 统计帧 | 模型调用均值 / P95 / P99 | 含解码离线吞吐 | 进程 RSS 峰值 | PyTorch 显存分配 / 缓存峰值 | 报告 |
| --- | --- | --- | ---: | ---: | ---: | --- |
| 猫狗检测 | 宠物 / 300 | 11.85 / 18.77 / 20.89 ms | 75.53 FPS | 1503.28 MB | 55.99 / 90 MB | [原始数据](cat_dog_v0.1/benchmark_results/test1_20260923_193234_389175/report.json) |
| 宠物动作 | 宠物 / 300 | 12.48 / 22.87 / 26.25 ms | 74.08 FPS | 1568.04 MB | 66.87 / 88 MB | [原始数据](pet_action_v0.1/benchmark_results/test1_20260923_193311_942847/report.json) |
| 运动员 + ByteTrack | 宠物 / 300 | 13.95 / 19.81 / 21.60 ms | 66.46 FPS | 1537.29 MB | 55.99 / 90 MB | [原始数据](yolo_bytetrack_v0.1/benchmark_results/test1_20260923_193401_520436/report.json) |
| 运动员 + ByteTrack | 网球 / 1482 | 11.67 / 19.13 / 20.21 ms | 76.14 FPS | 1560.21 MB | 62.99 / 88 MB | [原始数据](yolo_bytetrack_v0.1/benchmark_results/test_20260923_194036_729071/report.json) |

运动员模型在宠物视频中的已跟踪框数为 179/328 个检测框；在网球视频中为 3422/3436。**框数不等于跟踪准确率，也不能衡量 ID 切换次数**；网球场景的跟踪质量仍缺少标注真值与 ID 稳定性评估。

同一宠物视频上的猫狗检测 → 动作检测双模型测试，在**同一进程、同一完整帧上顺序执行**，没有检测框关联、动作融合或按目标裁剪：

| 指标 | 均值 | P95 | P99 |
| --- | ---: | ---: | ---: |
| 猫狗模型 | 11.11 ms | 19.80 ms | 21.74 ms |
| 动作模型 | 11.67 ms | 21.81 ms | 23.62 ms |
| 双模型计算 | 22.77 ms | 41.34 ms | 45.13 ms |
| 整帧含文件解码 | 23.78 ms | 42.59 ms | 46.48 ms |

该测试统计 300 帧，离线吞吐 **41.60 FPS**，进程 RSS 峰值 **1674.02 MB**，PyTorch 显存分配/缓存峰值 **76.8/98 MB**。[完整报告](pet_pipeline_results/test1_20260923_194350_988784/report.json)。两份单模型报告的 P95 不应直接相加；组合 P95 使用的是逐帧实测结果。

## 4. 定时回放：单/双/三模型压力测试

回放使用容量为 1 的“只留最新帧”队列。表中“选入”指发布给处理队列的 RGB 帧；“覆盖”指队列中等待处理的帧被新帧替换。`--process-fps` 引起的主动跳帧单独列出，**不计作队列覆盖**。所有 300 秒长测均模拟深度 30 FPS（9,000 次固定深度数组更新），模型输入尺寸 640。

| 编号 / 模式 | RGB 模拟 / 模型目标 | 选入 / 主动跳过 / 处理 | 队列覆盖 | 实际处理 | 模型处理均值 / P95 | 送帧到结果均值 / P95 | 报告 |
| --- | --- | --- | ---: | ---: | --- | --- | --- |
| T30：运动员 | 30 / 30 FPS | 303 / 0 / 303 | 0（0%） | 30.05 FPS | 15.39 / 22.68 ms | 15.50 / 22.74 ms | [T30](realtime_replay_results/tennis_tennis_1280x800_30fps_20260923_212953_207325/report.json) |
| T60：运动员 | 60 / 60 FPS | 18000 / 0 / 17827 | 173（0.96%） | 59.42 FPS | 12.81 / 19.66 ms | 15.03 / 25.76 ms | [T60](realtime_replay_results/tennis_tennis_1280x800_30fps_20260923_215234_171633/report.json) |
| P60-a：猫狗 + 动作 | 60 / 60 FPS | 18000 / 0 / 12887 | 5113（28.41%） | 42.95 FPS | 22.53 / 34.60 ms | 30.93 / 46.48 ms | [P60-a](realtime_replay_results/pet_tennis_1280x800_30fps_20260923_215808_766058/report.json) |
| P60-b：猫狗 + 动作 | 60 / 60 FPS | 18000 / 0 / 13410 | 4590（25.50%） | 44.70 FPS | 21.66 / 31.59 ms | 29.85 / 43.14 ms | [P60-b](realtime_replay_results/pet_tennis_1280x800_30fps_20260923_221349_678116/report.json) |
| A60：三模型 | 60 / 60 FPS | 18000 / 0 / 8834 | 9166（50.92%） | 29.44 FPS | 33.57 / 47.46 ms | 43.37 / 60.98 ms | [A60](realtime_replay_results/all_tennis_1280x800_30fps_20260923_222949_456338/report.json) |
| A30：三模型 | 60 / 30 FPS | 9000 / 9000 / 8094 | 906（10.07%） | 26.98 FPS | 36.08 / 51.22 ms | 49.13 / 72.28 ms | [A30](realtime_replay_results/all_tennis_1280x800_30fps_20260923_224339_907032/report.json) |
| A15：三模型 | 60 / 15 FPS | 4500 / 13500 / 4500 | 0（0%） | 15.00 FPS | 34.72 / 49.18 ms | 34.92 / 49.44 ms | [A15](realtime_replay_results/all_tennis_1280x800_30fps_20260923_225114_891918/report.json) |

T30 是约 10 秒的一轮回放，没有模拟深度长测；其余行是 300 秒测试。P60 的画面仍为网球场，仅可用于**粗略计算压力**，无法检验猫狗动作识别效果。A60/A30/A15 将同一 RGB 帧依次送入猫狗检测、宠物动作、运动员检测与 ByteTrack，**不是三个 GPU 模型并行推理**。

三模型每段的耗时在合跑报告中单独记录：

| 合跑档位 | 猫狗均值 / P95 | 动作均值 / P95 | 运动员 + ByteTrack 均值 / P95 | 合跑计算 P99 | 送帧到结果 P99 |
| --- | --- | --- | --- | ---: | ---: |
| A60 | 10.41 / 17.12 ms | 11.36 / 18.64 ms | 11.78 / 18.86 ms | 55.94 ms | 70.45 ms |
| A30 | 11.31 / 18.64 ms | 12.13 / 19.97 ms | 12.61 / 20.30 ms | 59.60 ms | 83.10 ms |
| A15 | 11.63 / 18.51 ms | 11.34 / 17.52 ms | 11.70 / 18.44 ms | 57.20 ms | 57.40 ms |

帧周期分别为：60 FPS **16.67 ms**、30 FPS **33.33 ms**、15 FPS **66.67 ms**。A30 的模型计算均值 36.08 ms、P95 51.22 ms，超过 30 FPS 周期；8,094 个已处理结果中，**7,183 个**晚于下一次 30 FPS 处理时刻。A15 送帧到结果 P95 49.44 ms、P99 57.40 ms，但最大值 81.94 ms，**4/4500 个**结果晚于下一次 15 FPS 处理时刻。A15 表明本电脑在这些测试条件下可持续处理约 15 FPS，不能证明板端同样可达。

## 5. 内存、显存、温度与功耗读数

| 合跑档位 | 进程 RSS 峰值 | PyTorch 显存分配 / 缓存峰值 | GPU 温度采样峰值 | GPU 功率采样峰值 |
| --- | ---: | ---: | ---: | ---: |
| A60 | 1775.79 MB | 86.79 / 104 MB | 63°C | 39.54 W |
| A30 | 1749.23 MB | 86.79 / 104 MB | 66°C | 39.96 W |
| A15 | 1755.41 MB | 86.79 / 104 MB | 61°C | 37.58 W |

读数的边界：RSS 是**该进程**的常驻内存，不含完整操作系统和所有后台程序；PyTorch 显存分配/缓存不等于整卡显存占用。`telemetry.csv` 的温度、GPU 功率等为周期采样，其中 GPU 功率是**笔记本独显**功率，不是整机耗电，也不能作为 Jetson 或小车供电规格。仅凭这些峰值不能断定有无热降频。逐帧耗时与内存见各测试目录的 `frames.csv`，周期资源采样见 `telemetry.csv`。

Jetson 上 CPU、GPU 与其他单元共用板载内存。现有三个权重合计 15.64 MB、电脑进程 RSS 峰值约 1.78 GB，说明**8 GB 板载内存值得实机尝试，但不能据此确认整机可用内存余量或实时性能**；系统、相机缓冲、TensorRT 引擎与工作区、视频处理和后续骨架模型都尚未计入板端测量。[NVIDIA Jetson 内存说明](https://docs.nvidia.com/cuda/archive/13.2.0/pdf/CUDA-For-Tegra-AppNote.pdf)

## 6. 相机输入与板端选型的可用约束

以下是**厂商标称规格或计算值，不是本项目实测**：

- [Orbbec Gemini 330 系列规格](https://www.orbbec.com/gemini-330-series/)列出 335L/336L 的 RGB 最高 `1280×800@60 FPS`、深度最高 `1280×800@30 FPS`、USB 3 Type-C、理想深度工作范围 `0.25–6 m`。页面分别给出各流上限；本项目还没有实机验证二者同时开启后的稳定性。
- 若 RGB 为未压缩 YUYV（2 字节/像素）且深度为 Y16（2 字节/像素），对应原始图像载荷约 **122.88 + 61.44 = 184.32 MB/s**，未计 USB 协议开销。MJPEG 的实际码率随画面内容变化。现有回放脚本只在电脑内存中复制图像数组，**没有模拟真实 USB 传输、曝光或深度计算**。
- 对三模型持续合跑，当前可将 **Jetson Orin NX 16GB** 作为开发候选；8GB 是否足够运行和稳定达到目标处理帧率需板端验证。NVIDIA 标称的内存、功率档位及 TOPS 不能直接换算成本项目 FPS。[NVIDIA Orin 功率与规格](https://developer.nvidia.com/blog/?p=95089)
- 板端应在**目标设备**上导出/验证三个模型的 TensorRT 引擎，再测整个 RGB → 三模型 → ByteTrack 链路。TensorRT 引擎对 GPU、CUDA 与运行时版本敏感。[Ultralytics Jetson 部署说明](https://docs.ultralytics.com/guides/nvidia-jetson)

## 7. 目前不能从这些测试推断的结果

1. **真实相机链路：**335L/336L 的 RGB 与深度双流是否能同时稳定在目标档位、相机输出和 USB 丢帧率、时间戳同步误差、曝光到结果的真实延迟。
2. **识别与跟踪质量：**网球视频没有宠物，宠物视频不能代表网球场；目标检测框数不是准确率，现有报告没有量化 ByteTrack 的 ID 切换。循环视频边界还会产生画面跳变。
3. **最终动作识别负载：**宠物动作训练为 960、压力测试为 640；未来骨架模型、网球动作分析、语音反馈、录像、目标关联、底盘控制均未进入当前合跑计时。
4. **板端运行能力：**电脑 PyTorch 的 FPS、RSS、显存和独显功率不能直接用于承诺 Orin Nano、Orin NX 或 AGX Orin 的 FPS、电池续航与散热表现。
5. **远距离网球跟拍：**335L/336L 的 `0.25–6 m` 是标称理想深度范围；运动员经常超过这个距离时，需要单独验证取景、目标像素尺寸和测距方案。

## 8. 建议的下一轮验收口径

先锁定“三模型同时运行”需要 **15 FPS 还是 30 FPS**，以及允许的相机到结果 P95、目标跟拍距离和供电预算。拿到候选板后，用相同模型、输入尺寸与真实目标场景视频，分别测 PyTorch 与目标板 TensorRT；记录已处理帧率、队列覆盖、模型 P95、完整链路 P95、整板内存、整板持续功耗与温度。拿到相机后再补 RGB/深度双流、时间戳同步和相机到结果的实测。对宠物动作还须在 640 与 960 两档同时比较**识别效果和性能**，才能最终确定模型输入尺寸。

## 附：其他已有动作视频测试

[宠物动作视频测试报告](pet_action_v0.1/video_test_results/report.json)另以 `imgsz=1440`、`conf=0.1` 对 3 个 30 FPS 视频共 2,511 帧进行了动作检测，汇总检测框 671 个（jumping 402、standing 124、running 61、sitting 35、walking 33、lying 16）。三段视频的报告平均推理耗时为 10.36、6.77、9.38 ms。该报告的测试程序、阶段定义和硬件环境信息不足以与上述标准性能报告直接比较；检测次数也不是动作识别准确率。
